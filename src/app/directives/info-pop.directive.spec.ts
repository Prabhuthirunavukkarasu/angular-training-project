import { InfoPopDirective } from './info-pop.directive';

describe('InfoPopDirective', () => {
  it('should create an instance', () => {
    const directive = new InfoPopDirective();
    expect(directive).toBeTruthy();
  });
});
